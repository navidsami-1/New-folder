#include<stdio.h>

int main() {
    int sum = 0;
    int n = 10;
// int age = 0;
// float height;

    // for (int i = 0; i < n; i++){
        for (int i = 1; i <= n; i++){
        sum = sum + i;
        // printf("Sum= %d\n", sum);
        // printf("Age=");
        // scanf("%d", &age);
        // printf("\nAge= %d\n", age);

        // printf("\nHeight=");
        // scanf("%d", &height);
        // printf("Height= %.1f\n", height);

    }
    printf("Sum= %d\n", sum);

    //while and do while.
}